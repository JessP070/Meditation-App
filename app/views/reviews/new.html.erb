<%=  display_header(@review)%>

<%= render partial: 'layouts/errors', locals: {object: @review} %>


<%= form_for(@review) do |f|%>
  <% if params[:meditation_id] %>
    <%= f.hidden_field :meditation_id %>
  <% else %>
    <div>
      <%= f.label :meditation_id, "Select a Meditation" %>
      <br>
    <%= f.collection_select :meditation_id, Meditation.alpha, :id, :mood_and_technique, include_blank: true %>
    </div>
  <% end %>
  <div>
    <%= f.label :stars %>
    <%= f.number_field :stars, min:0, max:5 %>
  </div>
  <br>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
   <br>
  <div>
    <%= f.label :content %>
    <%= f.text_area :content, size: "25x10" %>
  </div>
  
  <%= f.submit %>
<% end %>